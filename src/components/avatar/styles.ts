import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: 49,
    height: 49,
    alignItems: 'center',
    justifyContent: 'center',
    marginRight: 22,
    borderRadius: 8,
  },
  avatar: {
    width: 45,
    height: 45,
    borderRadius: 8,
  },
})
